# Additional UI Assets — manifest

No external artwork is required for the current production UI.

- Map location art: rendered from the real rooms
  (`assets/ui/map/vignette_{library,garden,train,japanese}.png`, 720×560).
- Map fallback: drawn placeholder (`assets/ui/placeholders/vignette_placeholder.png`,
  720×560) auto-used if a vignette is ever missing.
- Icons: ASCII-safe glyphs + theme-drawn controls (no icon files needed).
- Brand: text wordmark (no logo file needed).

The JSON below mirrors this: an empty required list.

```json
[]
```
